"use client";
export default function PrintButton({ label = "🖨️ طباعة PDF" }: { label?: string }) {
  return (
    <button onClick={() => window.print()}
      className="rounded-lg bg-gray-800 text-white px-4 py-2 text-sm hover:bg-gray-700">
      {label}
    </button>
  );
}
